config.h:/* This is how to allocate empty space in some section.  The .zero
config.h:  func_ptr *p, *beg = alloca (0);				\
config.h:  func_ptr *p, *beg = alloca (0);				\
config.h:/* Store in OUTPUT a string (made with alloca) containing
config.h:( (OUTPUT) = (char *) alloca (strlen ((NAME)) + 10),	\
defaults.h:/* Store in OUTPUT a string (made with alloca) containing
defaults.h:    char *temp = (char *) alloca (len + 3);				\
defaults.h:    (OUTPUT) = (char *) alloca (strlen (NAME) + 11);			\
